# Project: v1.x Downstream Project (Markdown Era)

This project was created with bigpowers v1.x, before the YAML cockpit was introduced.
It uses Markdown files: STATE.md, RELEASE-PLAN.md, SCOPE.md, CONTEXT.md.

## Stack

- Language: TypeScript
- Runtime: Node.js

## Tests

Run `npm test` for the test suite.
