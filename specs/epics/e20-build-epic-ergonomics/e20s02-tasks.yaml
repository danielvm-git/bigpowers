story_id: e20s02
title: "state churn reduction"
status: backlog
bcps: 1
tasks:
  - id: 1
    description: "Add --squash-state flag to release-branch/SKILL.md for squashing chore(state): commits"
    verify: "grep -c 'squash.state\|--squash.state' release-branch/SKILL.md | awk '{if(\$1>=1) print \"OK\"; else print \"FAIL\"}'"
    status: backlog
  - id: 2
    description: "Document state commit policy in CONVENTIONS.md"
    verify: "grep -c 'state.*commit\|state.*squash\|state.*policy' CONVENTIONS.md | awk '{if(\$1>=1) print \"OK\"; else print \"FAIL\"}'"
    status: backlog
