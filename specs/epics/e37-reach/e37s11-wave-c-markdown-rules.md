# story: e37s11
# targets.yaml — Wave C: qwen, kilocode (markdown commands + rules)

## Acceptance Criteria

grep -q 'qwen' scripts/targets.yaml && grep -q 'kilocode' scripts/targets.yaml && echo OK

