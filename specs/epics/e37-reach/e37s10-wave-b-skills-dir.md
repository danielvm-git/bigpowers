# story: e37s10
# targets.yaml — Wave B: zed, omp, hermes (skills-dir adapters)

## Acceptance Criteria

grep -q 'zed' scripts/targets.yaml && grep -q 'omp' scripts/targets.yaml && grep -q 'hermes' scripts/targets.yaml && echo OK

