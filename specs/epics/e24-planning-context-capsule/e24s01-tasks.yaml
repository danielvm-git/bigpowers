story_id: e24s01
title: "elaborate-spec writes planning-context.yaml"
status: backlog
bcps: 1
tasks:
  - id: 1
    description: "Add planning-context.yaml output section to elaborate-spec/SKILL.md with schema and write-at-end instruction"
    verify: "grep -c 'planning-context\\|planning.context' elaborate-spec/SKILL.md | awk '{if($1>=1) print \"OK\"; else print \"FAIL\"}'"
    status: backlog
  - id: 2
    description: "Document conflict handling (file already exists) in elaborate-spec/SKILL.md"
    verify: "grep -q 'already exists\\|overwrite\\|Update existing' elaborate-spec/SKILL.md && echo OK || echo FAIL"
    status: backlog
