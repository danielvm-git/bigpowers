story_id: e54s02
title: "Add a soft drift-detection gate for the freeze window"
status: todo
bcps: 3
tasks:
  - id: 1
    description: "Write scripts/check-catalog-drift.sh — diff live skills/*/SKILL.md against the newest specs/tech-architecture/CATALOG-BASELINE-*.yaml, classify addition/removal/structural-change, print WARNING lines, always exit 0"
    verify: "bash scripts/check-catalog-drift.sh >/dev/null 2>&1; echo OK"
    status: todo
  - id: 2
    description: "Handle the no-baseline case gracefully per e54s02 §6c: print 'no baseline found, skipping' and exit 0 rather than erroring"
    verify: "mkdir -p /tmp/no-baseline-test && (cd /tmp/no-baseline-test && bash /Users/danielvm/Developer/bigpowers/scripts/check-catalog-drift.sh 2>&1 | grep -qi 'no baseline' ) && echo OK"
    status: todo
  - id: 3
    description: "Wire the check into scripts/run-verification-gates.sh or Preflight as an informational step — visible every run, never affecting the gate's own exit code"
    verify: "grep -q 'check-catalog-drift' scripts/run-verification-gates.sh CLAUDE.md 2>/dev/null && echo OK"
    status: todo
  - id: 4
    description: "Recognize the exception marker documented in e54s03 and suppress matching warnings"
    verify: "grep -qi 'exception' scripts/check-catalog-drift.sh && echo OK"
    status: todo
