story_id: e18s02
title: "enforce-first integration"
status: backlog
bcps: 1
tasks:
  - id: 1
    description: "Add --quick mode to enforce-first/SKILL.md (Fast + Independent + Self-Validating checks only)"
    verify: "grep -c 'quick\|--quick\|fast.mode' enforce-first/SKILL.md | awk '{if(\$1>=1) print \"OK\"; else print \"FAIL\"}'"
    status: backlog
  - id: 2
    description: "Add enforce-first invocation to build-epic step 6 as sub-check after audit-code"
    verify: "grep -c 'enforce-first' build-epic/SKILL.md | awk '{if(\$1>=1) print \"OK\"; else print \"FAIL\"}'"
    status: backlog
