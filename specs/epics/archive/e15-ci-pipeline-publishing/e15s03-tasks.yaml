story_id: e15s03
title: "CI verification and dry-run"
status: backlog
bcps: 2
tasks:
  - id: 1
    description: "Add CI verification step (8b) to release-branch/SKILL.md — gh run list polling with 10min timeout"
    verify: "grep -c 'gh run list\|CI verification' release-branch/SKILL.md | awk '{if(\$1>=2) print \"OK\"; else print \"FAIL\"}'"
    status: backlog
  - id: 2
    description: "Add CI dry-run sub-step to develop-tdd/SKILL.md — actionlint/yamllint + pitfall checks"
    verify: "grep -c 'workflow\|actionlint\|pitfall.*ci' develop-tdd/SKILL.md | awk '{if(\$1>=1) print \"OK\"; else print \"FAIL\"}'"
    status: backlog
  - id: 3
    description: "Document CI failure handoff pattern — release-branch routes to fix-bug on CI failure"
    verify: "grep -c 'fix.bug\|handoff.*ci\|failure.*url' release-branch/SKILL.md | awk '{if(\$1>=1) print \"OK\"; else print \"FAIL\"}'"
    status: backlog
