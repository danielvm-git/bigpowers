story_id: e51s05
title: "CLAUDE.md — solo-default agent rules + bigpowers Preflight command row"
status: done
bcps: 2
risk: P1
tasks:
  - id: 1
    description: >-
      Add Preflight row to CLAUDE.md Commands table (compliance, golden suite,
      workspace tests, sync-skills, trace --strict) and CI row (gh pr checks).
    verify: grep -qi 'Preflight' CLAUDE.md && grep -qi 'run-golden-suite' CLAUDE.md && echo OK
    status: done
  - id: 2
    description: >-
      Replace Agent Rules scope-minimization with Always Green / fix-or-log mandate;
      reference CONVENTIONS § Discovered Defects. Remove the old "never refactor
      outside task scope" line that permits defect dismissal.
    verify: >-
      grep -q 'Always Green' CLAUDE.md &&
      grep -q 'fix-or-log' CLAUDE.md &&
      ! grep -q 'Never refactor, rename, or reorganize code outside the task scope' CLAUDE.md && echo OK
    status: done
  - id: 3
    description: >-
      Update seed-conventions REFERENCE.md Never section to match (never dismiss
      reproducible defects; never proceed on red Preflight).
    verify: grep -qi 'red Preflight\|discovered defect' skills/seed-conventions/REFERENCE.md && echo OK
    status: done
  - id: 4
    description: >-
      Add `# story: e51s05` tag to CLAUDE.md header for trace-stories coverage.
    verify: "grep -q 'story: e51s05' CLAUDE.md && echo OK"
    status: done
