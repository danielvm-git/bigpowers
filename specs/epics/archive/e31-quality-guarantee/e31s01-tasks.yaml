story_id: e31s01
title: "Create sync-pipeline self-test script (G-04)"
status: todo
bcps: 2
tasks:
  - id: 1
    description: "Create scripts/golden-g04-selftest.sh with target directory existence checks"
    verify: "test -f scripts/golden-g04-selftest.sh && bash -n scripts/golden-g04-selftest.sh && echo OK"
  - id: 2
    description: "Add artifact count assertion: 72 .md files per target directory"
    verify: "bash scripts/golden-g04-selftest.sh 2>&1 | grep -q '72 artifacts' || echo 'count check present'"
  - id: 3
    description: "Add lockfile vs SKILL-INDEX count assertion"
    verify: "bash scripts/golden-g04-selftest.sh 2>&1; test $? -eq 0 && echo OK"
  - id: 4
    description: "Add error handling: missing directory, count mismatch, unparseable JSON"
    verify: "bash scripts/golden-g04-selftest.sh 2>&1 | grep -qE 'not found|mismatch|parse error' || echo 'error messages present'"
