# story: e37s10
# targets.yaml — Wave B: zed, omp, hermes (skills-dir adapters)

## Acceptance Criteria

```bash
grep -q 'zed' scripts/targets.yaml &&
grep -q 'omp' scripts/targets.yaml &&
grep -q 'hermes' scripts/targets.yaml &&
echo OK
```

## Out of scope
- Context derivatives for these targets (generated by e37s06 for all registry targets)
- Skill sync beyond existing sync-skills.sh dispatch (e37s07)
- Adapter hook implementation beyond registry row + adapter file skeleton

## Adapter guidance
- zed: skills-dir adapter — `skill.adapter: zed`, `context.mode: copy` (zed reads rules directory)
- omp (Open Multi-Process): skills-dir adapter — `skill.adapter: omp`, `context.mode: symlink`
- hermes: skills-dir adapter — `skill.adapter: hermes`, `context.mode: config-bridge` if hermes requires config, else `symlink`
- Test gate: `bash scripts/test-adapters.sh zed && bash scripts/test-adapters.sh omp && bash scripts/test-adapters.sh hermes` must pass

