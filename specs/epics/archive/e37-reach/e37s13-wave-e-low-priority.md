# story: e37s13
# targets.yaml — Wave E: iflow, vibe, shai (markdown commands; optional)

## Acceptance Criteria

grep -q 'iflow' scripts/targets.yaml && grep -q 'vibe' scripts/targets.yaml && grep -q 'shai' scripts/targets.yaml && echo OK

