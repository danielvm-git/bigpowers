story_id: e37s09
title: "targets.yaml — Wave A: Goose (OSS) + Antigravity agy (proprietary beta)"
status: done
bcps: 3
risk: P2
depends_on:
  - e37s05
  - e37s07
  - e37s08
tasks:
  - id: 1
    description: >-
      Add goose target row to scripts/targets.yaml: id: goose, label: "Goose
      (Block OSS)", context.mode: native (reads AGENTS.md natively — no adapter
      file required), tier: default_on. Include a comment citing Goose's
      AGENTS.md instruction file support.
      Tag: # story: e37s09, # scenario: SC-e37s09-P2-01.
    verify: >-
      grep -q 'goose' scripts/targets.yaml &&
      bash scripts/validate-targets-yaml.sh && echo OK
    risk: P2
    security: none
    status: done

  - id: 2
    description: >-
      Add agy target row to scripts/targets.yaml: id: agy, label: "Antigravity
      agy (proprietary beta)", tier: opt_in, context.mode: symlink (same pattern
      as pi adapter). Note proprietary beta — no runtime CI verification.
    verify: >-
      grep -q 'agy' scripts/targets.yaml &&
      bash scripts/validate-targets-yaml.sh && echo OK
    risk: P2
    security: none
    status: done

  - id: 3
    description: >-
      Run bash scripts/test-adapters.sh goose; confirm PASS. (goose is native
      reader — test confirms AGENTS.md present and registry row valid, not that
      goose binary is installed.)
    verify: >-
      bash scripts/test-adapters.sh goose && echo OK
    risk: P2
    security: none
    status: done

  - id: 4
    description: >-
      Story verify: goose in targets.yaml + matrix PASS for goose row.
    verify: >-
      grep -q 'goose' scripts/targets.yaml &&
      bash scripts/verify-install.sh --matrix 2>&1 | grep -q 'goose.*PASS' && echo OK
    risk: P2
    security: none
    status: done

out_of_scope:
  - "Adapter file creation for Goose (uses native AGENTS.md — no adapter)"
  - "Skill symlinks for instruction-file-only tools"
  - "Runtime E2E with goose binary"
  - "agy runtime verification (proprietary beta — registry row only)"
