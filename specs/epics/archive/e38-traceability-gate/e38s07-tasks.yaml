story_id: e38s07
title: "Wire gate-trace into release-branch pre-PR gate"
status: todo
bcps: 1
tasks:
  - id: 1
    description: "Add gate-trace invocation to release-branch SKILL.md before PR creation"
    verify: "grep -q 'gate-trace' skills/release-branch/SKILL.md && echo OK"
  - id: 2
    description: "FAIL blocks merge; CONCERNS requires explicit human override in state.yaml"
    verify: "grep -q 'CONCERNS.*override|FAIL.*block' skills/release-branch/SKILL.md && echo OK"
