story_id: e17s01
title: "craft-skill: validate-contracts"
status: backlog
bcps: 3
tasks:
  - id: 1
    description: "Create validate-contracts/SKILL.md with verb-noun naming, frontmatter, and overview of the three contract modes (schema, key-set, shape)"
    verify: "test -f validate-contracts/SKILL.md && grep -q 'name: validate-contracts' validate-contracts/SKILL.md && echo OK"
    status: backlog
  - id: 2
    description: "Define specs/contracts/ directory convention and document YAML schema for all three contract types: API schema validation, key-set comparison, data shape contracts"
    verify: "grep -qi 'specs/contracts\|JSON Schema\|key.set\|data.shape' validate-contracts/SKILL.md && echo OK"
    status: backlog
  - id: 3
    description: "Document all three validation modes with generic examples (not project-specific), CLI usage, and exit code semantics"
    verify: "grep -ci 'divergence\|missing key\|type mismatch\|diff\|conforms\|column' validate-contracts/SKILL.md | awk '{if(\$1>=3) print \"OK\"; else print \"FAIL\"}'"
    status: backlog
  - id: 4
    description: "Document CI integration (JSON Lines output) and pre-deploy gate integration with the deploy skill"
    verify: "grep -ci 'JSON Lines\|machine.parse\|CI\|deploy.*gate\|pre.deploy' validate-contracts/SKILL.md | awk '{if(\$1>=2) print \"OK\"; else print \"FAIL\"}'"
    status: backlog
  - id: 5
    description: "Add validate-contracts to SKILL-INDEX.md via sync-skills.sh"
    verify: "grep -q 'validate-contracts' SKILL-INDEX.md && echo OK"
    status: backlog
