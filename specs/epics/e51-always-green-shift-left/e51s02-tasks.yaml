story_id: e51s02
title: "seed-conventions — Preflight default, solo-git default, embed Always Green template"
status: todo
bcps: 3
risk: P1
tasks:
  - id: 1
    description: >-
      Add interview question for Preflight command; if user says nothing, chain
      Test + Lint + Build answers into Preflight row in CLAUDE.md Commands table.
    verify: grep -qi 'Preflight' skills/seed-conventions/SKILL.md && echo OK
    status: todo
  - id: 2
    description: >-
      Update REFERENCE.md agent-config template: Preflight + CI rows; embed Always
      Green and banned phrases in CONVENTIONS template fragment.
    verify: grep -qi 'Preflight' skills/seed-conventions/REFERENCE.md && grep -q 'Always Green' skills/seed-conventions/REFERENCE.md && echo OK
    status: todo
  - id: 3
    description: >-
      Change specs/state.yaml default workflow_mode from team-pr to solo-git in
      seed-conventions Generate files section.
    verify: grep -q 'solo-git' skills/seed-conventions/SKILL.md && echo OK
    status: todo
  - id: 4
    description: Run bash scripts/sync-skills.sh after SKILL.md and REFERENCE.md edits.
    verify: bash scripts/sync-skills.sh >/dev/null && grep -qi 'Preflight' .cursor/rules/seed-conventions.mdc && echo OK
    status: todo
