story_id: e18s01
title: "audit-code as hard gate in build-epic step 6"
status: done
bcps: 2
tasks:
  - id: 1
    description: "Add --gate mode to audit-code/SKILL.md (non-zero exit, pass/fail JSON/YAML output)"
    verify: "grep -c 'gate\\|--gate\\|non-zero' audit-code/SKILL.md | awk '{if($1>=2) print \"OK\"; else print \"FAIL\"}'"
    status: done
  - id: 2
    description: "Modify build-epic/SKILL.md step 6 to invoke audit-code unconditionally with fail loopback"
    verify: "grep -c 'audit-code' build-epic/SKILL.md | awk '{if($1>=3) print \"OK\"; else print \"FAIL\"}'"
    status: done
  - id: 3
    description: "Add audit_result persistence in state.yaml and AUDIT report artifact path"
    verify: "grep -c 'audit_result' build-epic/SKILL.md | awk '{if($1>=1) print \"OK\"; else print \"FAIL\"}'"
    status: done
  - id: 4
    description: "Add enforce-first --quick sub-check after audit-code passes in build-epic step 6"
    verify: "grep -c 'enforce-first' build-epic/SKILL.md | awk '{if($1>=1) print \"OK\"; else print \"FAIL\"}'"
    status: done
