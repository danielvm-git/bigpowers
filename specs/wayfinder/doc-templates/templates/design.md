<!-- wayfinder resolution artifact — T22 (design-and-spike), closed -->
<!-- DEFERS TO AN EXTERNAL SPEC — does not invent one. DESIGN.md implements Google's own
     published `design.md` format, validated by `npx @google/design.md lint`. The 8 body
     sections are that spec's authority, not bigpowers'; do not hand-author section content here
     that might drift from what the real linter expects. This template only adds the bigpowers-
     specific conventions layered on top: the OKF envelope and the uncertainty-flagging comment. -->
<!-- NOT the T13 mechanical-vs-curated split. Unlike tech-stack.md (terse "Long-Term Memory,"
     wrong register for a reader), DESIGN.md's prose (Overview, Do's/Don'ts) is generated FROM
     extracted tokens specifically to be read by humans and agents — it's already the right
     register, no separate curated companion needed. -->

---
okf_kind: design
okf_version: "1.0"
generated_by: "skill:extract-design"
generated_at: {YYYY-MM-DDTHH:MM:SSZ}
source: {HTML prototype path or URL this was extracted from}
---

{The 8 sections below are generated by the extraction pipeline (colors, typography, spacing,
rounded, components — Material 3 token roles — plus AI-generated Overview and Do's/Don'ts prose).
Do not hand-write their structure here; run `node extract-design/scripts/extract.js` and validate
with `npx @google/design.md lint` rather than filling in a static template.}

{Every low-confidence classification or AI-generated prose assertion carries:}

<!-- AGENT NOTE: uncertain — validate during grill-me. Evidence: {what was observed} -->

{This flagging convention is bigpowers-specific, layered on top of the external spec — keep it
even where the external spec doesn't require it.}
