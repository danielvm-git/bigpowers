name: check-stack
command: /check-stack
description: Health-check the project state and assess impact before any change.
skills:
  - survey-context
  - assess-impact
  - setup-environment
verify: "ls survey-context/SKILL.md assess-impact/SKILL.md setup-environment/SKILL.md >/dev/null 2>&1 && echo 'OK' || echo 'FAIL'"
