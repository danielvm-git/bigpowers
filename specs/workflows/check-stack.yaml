name: check-stack
command: /check-stack
description: Health-check the project state and assess impact before any change.
skills:
  - survey-context
  - assess-impact
  - verify-work
verify: "grep -q 'survey-context' specs/workflows/check-stack.yaml && echo OK"
