name: security
command: /security
description: Security-focused audit then independent reviewer for OWASP top-10 and supply chain.
skills:
  - audit-code
  - request-review
verify: "ls audit-code/SKILL.md request-review/SKILL.md >/dev/null 2>&1 && echo 'OK' || echo 'FAIL'"
