name: e2e
command: /e2e
description: End-to-end gate — smoke test the live deployment then full UAT verification.
skills:
  - smoke-test
  - verify-work
verify: "ls smoke-test/SKILL.md verify-work/SKILL.md >/dev/null 2>&1 && echo 'OK' || echo 'FAIL'"
