name: tdd
command: /tdd
description: Test-driven development loop with F.I.R.S.T enforcement after each test cycle.
skills:
  - develop-tdd
  - enforce-first
verify: "ls develop-tdd/SKILL.md enforce-first/SKILL.md >/dev/null 2>&1 && echo 'OK' || echo 'FAIL'"
