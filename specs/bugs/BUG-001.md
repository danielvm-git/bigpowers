---
bug_id: BUG-001
status: fixed
severity: high
scope: planning
title: "Epic Status Drift in YAML files"
files_changed: "specs/epics/archive/e12-pi-support/epic.yaml, specs/epics/archive/e18-quality-gates-impact/epic.yaml, specs/epics/archive/e19-quick-fix-fast-path/epic.yaml, specs/epics/archive/e26-security-review/epic.yaml, specs/epics/archive/e27-specs-naming-convention/epic.yaml, specs/epics/archive/e29-skills-directory/epic.yaml, tests/test-epic-status-drift.sh"
approach: "Set epic-level status: done where execution-status.yaml already marks epic done; add regression test"
commit_message: "fix(planning): align epic.yaml status with execution-status for completed epics"
---

# BUG-001: Epic Status Drift

## Issue

Epic capsule `status` fields in `epic.yaml` were out of sync with `specs/execution-status.yaml` (authoritative for completed work). Original report named e28, e34, e38, e39, e40, e47 — those were already `done` after archive migration.

## Root Cause

Epic YAML files were not updated to `done` after stories merged; archive moves fixed some paths but left stale top-level status on e12, e18, e19, e26, e27, e29.

## Fix Applied

| Epic | Before | After |
|------|--------|-------|
| e12 | in_progress | done |
| e18 | in_progress | done |
| e19 | backlog | done |
| e26 | planned | done |
| e27 | not-started | done |
| e29 | todo | done |

e28, e34, e38, e39, e40, e47 — already `done` (no change).

**Excluded:** e37 remains `planned` in `epics/e37-reach/epic.yaml` — active next epic per release plan; execution-status `done` reflects prior e37 BCP work superseded by Reach redesign.

## Regression Test

```bash
bash tests/test-epic-status-drift.sh
```

Compares `execution-status.yaml` `done` epics against matching `epic.yaml` top-level `status` (skips e37 by design).

## Acceptance Criteria

- [x] All originally named epics (e28, e34, e38, e39, e40, e47) show `status: done`
- [x] Additional drift epics (e12, e18, e19, e26, e27, e29) updated to `done`
- [x] `bash tests/test-epic-status-drift.sh` exits 0
- [x] `bash scripts/run-verification-gates.sh` still PASS
