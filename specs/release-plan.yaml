release:
  # NOTE: version is set by semantic-release at merge time from Conventional Commits.
  # Do NOT track it manually here — this field mirrors the real tag for reference only.
  # Real source of truth: `gh release view` / git tags. Last synced: v2.56.1 (2026-07-03).
  version: "2.56.1"   # mirror, not the authority
  codename: "Deepening"
  status: released
  semantic_release: true
  bump_hint: minor
release_trains:
  # Proposed public-launch sequencing for the current backlog (e32-e40),
  # grouped by value + dependency rather than raw WSJF order. Resolves the
  # e32-vs-e34 "what's next" decision open in specs/state.yaml handoff.
  # Written 2026-07-03 in response to: "plan a meaningful flow of launches
  # for the public." Epic-level detail unchanged below — this only sequences.
  - train: "v2.5x — Trust & Signal"
    epics: [e40, e34]
    total_bcps: 28
    rationale: >
      e40 is urgent, not optional: release-branch is writing corrupted
      cycle-time rows on every story that lands today, so every day of
      delay adds more rows for e40s07 to quarantine later. e34 is a cheap,
      dependency-free doc/frontmatter win that keeps cadence alongside it.
      Neither is public-facing — this train is pure internal debt-paydown.
  - train: "v2.6x — Traceability Gate"
    epics: [e38, e33]
    total_bcps: 26
    rationale: >
      e38 is the single highest-WSJF item in the backlog (3.50) with no
      blockers. e33 is pulled forward out of its own WSJF rank (2.0)
      because its render-target refactor is an explicit architectural
      prerequisite for e39's OKF Phase 2 (render_okf_bundle()) — landing
      it here avoids e39 stalling mid-epic on a missing dependency.
  - train: "v2.7x/v3.0 — Semantic Bridge (headline release)"
    epics: [e43, e39, e44, e28, e41, e42, e37]
    total_bcps: 87
    rationale: >
      e43 (highest WSJF at 4.00, previously unslotted) leads this train —
      its MCP server is the foundation e39s01 now emits into, avoiding a
      build-then-deprecate cycle. e39+e44 depend on e43's entity-relation
      graph and e40's OKF pipeline (landed in v2.5x). e28 (Docs Website)
      is the audience-facing site that turns e39's internal graph into a
      public artifact. e41 renders receipts evidence as a live page.
      e42 (pulled from v2.8x) ships site + receipts + worked example
      together — the only epic that answers "why adopt this?". e37 caps
      the train with golden story maturation. Candidate for a v3.0 launch
      note — first release in this backlog where a stranger can open a
      browser, see evidence, and follow a worked example.
  - train: "v2.8x — Docs Polish"
    epics: [e32, e36, e45, e46]
    total_bcps: 48
    rationale: >
      Both are content-quality and non-blocking — safe to land whenever
      cadence allows. Sequenced last deliberately: land AFTER the v2.7x
      docs site goes live so the newly-public site doesn't launch showing
      stale or duplicated reference docs. e42 (Showcase Repo, added
      2026-07-03) joins this train so its spec trail demonstrates the
      post-e40 methodology, not the deprecated hand-arithmetic pipeline.
      e46 (Risk-Based Verification) slots here: depends on e42's verify
      commands as the test surface being scaled.
  drift_note: >
    e26 (Security-Review Lifecycle) is still marked "ACTIVE — next release
    (v2.30.0)" in the epics list below, but execution-status.yaml shows all
    7 of its stories done. That's stale status, not a forward-planning item
    — needs `bash scripts/sync-status-from-epics.sh`, not a release train.
epics:
  # ⚠ DO NOT RENUMBER EPIC IDs. IDs are identifiers, not sequence.
  # Epic IDs are woven into story tags in code (story: eNNsNN), the
  # traceability matrix, cycle-times.yaml, git history, and GitHub issues.
  # Renumbering would corrupt the e38 traceability gate — hundreds of
  # orphaned story tags. The release_trains section above is the
  # sequencing artifact. IDs are immutable; execution order is mutable.
  #
  # ── BACKLOG — queued (pre-deepening) ─────────────────────────────────
  - id: e29
    title: "Repository Layout — Skill Sources Under skills/"
    wsjf: 3.3
    bcps: 7
    capsule_dir: epics/e29-skills-directory
    note: >
      Move all skill dirs from repo root into skills/ so the GitHub landing
      page shows the README (~96 root entries → ~20). Location-agnostic
      script discovery first, then atomic move + regeneration (npm
      postinstall coupling forbids a split release). WSJF (BV 5 + TC 3 +
      RR-OE 2) / JS 3 = 3.3 — outranks e28, which must build its site
      generator against the new skills/ layout, not */SKILL.md.
  - id: e28
    title: "Docs Website — Fourth Generated Artifact Target"
    wsjf: 2.8
    bcps: 13
    capsule_dir: epics/e28-docs-website
    note: >
      Astro Starlight site in website/ (same repo), pages generated at build
      time from README.md, */SKILL.md, docs/, and specs/adr; deployed to
      GitHub Pages via Actions. Zero hand-maintained site content — the site
      is a fourth generated artifact target alongside .cursor/.gemini/.pi.
      WSJF (BV 7 + TC 3 + RR-OE 4) / JS 5 = 2.8 — outranks e27 (2.0), does
      not disrupt active e26.
      OKF alignment: OKF bundles are Astro/MkDocs-ready markdown + frontmatter.
      The docs website could render specs/skills-wiki/ and specs/codebase-wiki/
      as browsable pages. See specs/IMPACT-e38-okf-adoption.md.
  - id: e27
    title: "Specs Output Naming Convention — _LATEST.md suffix"
    wsjf: 2.0
    bcps: 7
    capsule_dir: epics/e27-specs-naming-convention
    note: >
      Rename all fixed-name generated outputs in specs/ root to use _LATEST.md
      suffix (7 skills + build-skill-index.sh). Does not touch date-stamped or
      feature-stamped files.

  # ── PLANNED — v2.45.0 "Deepening" release ───────────────────────────
  # Source: specs/RELEASE-PLAN-v2.45.0-DEEPENING.md (full plan; e31 split
  #         amended 2026-07-02 per specs/PLAN-AUDIT_LATEST.md)
  #         specs/DEEPEN-ARCHITECTURE-REVIEW.md (review)
  #         specs/QUALITY-GUARANTEE-STRATEGY.md (golden suite)
  # Build order (superseded by release_trains above, 2026-07-03):
  #   v2.5x: e40 → e34   |   v2.6x: e38 → e33   |
  #   v2.7x/v3.0: e39 → e28 → e37s02-s04   |   v2.8x: e32 → e36
  # (e37 spike PASSED 2026-07-03 via gh-aw + DeepSeek; G-01 golden story live.
  #  e37s02-s04 no longer spike-gated — can proceed after e31 builds the gates.
  #  e28 pulled forward from its old pre-deepening backlog slot into v2.7x.)
  # ── DONE — shipped as v2.50.0 (Deepening, 2026-07-03) ────────────────
  - id: e31
    title: "Quality Guarantee — Deterministic Gates"
    wsjf: 9.5
    bcps: 11
    capsule_dir: epics/archive/e31-quality-guarantee
    note: >
      Delivered: G-04 sync-pipeline self-test, compliance CI gate, golden suite
      runner (--baseline, --check-size), pre-merge mandate, evolve-skill regression
      gate. 7 stories across 4 PRs (#43-#46). Replaced by e37 golden stories as
      consumer of these gates.
  - id: e32
    title: "Missing Historical References"
    wsjf: 3.0
    bcps: 20
    capsule_dir: epics/e32-historical-refs
    note: >
      9 reference docs: Beck (XP/Tidy First), Fowler (refactoring catalog),
      Feathers (seams/characterization), Metz (POODR/SOLID), Hickey (simple
      vs easy), Hunt & Thomas (Pragmatic Programmer), Evans (DDD), Forsgren
      (Accelerate/DORA), Larson (Staff Engineer).
  - id: e34
    title: "Context Engineering Layer"
    wsjf: 2.75
    bcps: 10
    capsule_dir: epics/e34-context-engineering
    note: >
      Name the 4 context-engineering strategies bigpowers already implements
      (write/select/compress/isolate), add effort: heavy|light frontmatter.
      Borrows GSD Core's central insight.
  - id: e35
    title: "DORA Metrics Extension"
    wsjf: 2.67
    bcps: 7
    status: superseded
    superseded_by: e40
    capsule_dir: epics/e35-dora-metrics
    note: >
      SUPERSEDED by e40 (2026-07-03) — e35's premise (extend the hand-written
      cycle-times.yaml) rested on the unreliable metric e40 replaces. Kept
      here for history; excluded from all release trains. Not part of the
      build queue. Original scope: extend cycle-times.yaml from BCP/hr only
      to all four DORA keys: deployment frequency, lead time, change failure
      rate, restore time — absorbed into e40's design.
  - id: e33
    title: "Sync Pipeline Refactor"
    wsjf: 2.0
    bcps: 13
    capsule_dir: epics/e33-sync-pipeline
    note: >
      Refactor sync-skills.sh from copy-paste-variant to parse→IR→render.
      Extract scripts/lib/skill-common.sh shared library. Render-target
      functions: adding IDE target = adding a function, not editing loop.
      OKF alignment: new render_okf_bundle() target maps perfectly to this
      architecture. See specs/IMPACT-e38-okf-adoption.md §Phase 2.
  - id: e36
    title: "Documentation Deduplication"
    wsjf: 1.4
    bcps: 10
    capsule_dir: epics/e36-doc-dedup
    note: >
      Convert reference docs from restating principles to provenance pointers.
      CONVENTIONS.md becomes single source of truth. F.I.R.S.T rubric lives
      in one place, not six.
      OKF alignment: the thin-provenance pattern (concept body = pointer to
      authority doc) is exactly e36's goal applied to OKF concept documents.
      See specs/IMPACT-e38-okf-adoption.md.
  - id: e37
    title: "Golden Story Suite — Agent-Driven"
    wsjf: 1.8
    bcps: 13
    capsule_dir: epics/e37-golden-stories
    note: >
      Agent-execution half of the original e31 (split per PLAN-AUDIT
      2026-07-02). SPIKE PASSED 2026-07-02: DeepSeek v4 via gh-aw Claude
      engine confirmed viable (ANTHROPIC_BASE_URL → api.deepseek.com/anthropic).
      G-01 golden story live at .github/workflows/e37-golden-deepseek.lock.yml.
      e37s01 done; s02-s04 proceed. Depends on e31 (deterministic gates).
      Per-epic cadence, pass@k 2-of-3 flake policy.
  - id: e38
    title: "Spec-to-Code Traceability Gate"
    wsjf: 3.50
    bcps: 13
    capsule_dir: epics/e38-traceability-gate
    note: >
      The "missing link" — deterministic spec-to-code traceability spanning
      three tiers: matrix builder (scripts/trace-stories.sh), blind-spot
      heuristic detector, and quality gate decision. TEA-inspired oracle
      confidence downgrade (tagged→high, heuristic→medium, inferred→low).
      Machine-readable JSON for CI/CD. Source: market survey of speckit-utils,
      V-Model trace, BMAD TEA bmad-testarch-trace (2026-07-02).
  - id: e39
    title: "Semantic Context Bridge"
    wsjf: 3.60
    bcps: 20
    capsule_dir: epics/e39-knowledge-graph
    note: >
      Closes remaining Layer 3 (Technical Context) and Layer 5 (Product Surface)
      gaps after e38 traceability gate. Three pillars: skill reference graph
      (parse all SKILL.md → directed dependency graph), agent coordination
      lockfile (prevent multi-agent collision), spec drift detection (suspect
      links when requirements change). OKF Phases 2-4: skills-wiki, conventions-
      wiki, agent-guide from sync-skills.sh + new maintain-wiki skill for
      agent-maintained language-agnostic code understanding + validate-okf.sh
      CI conformance gate. Source: Karpathy LLM Wiki, OKF v0.1, Google
      Knowledge Catalog, IMPACT-e38-okf-adoption.md, session 2026-07-02/03.
  - id: e43
    title: "MCP Semantic Context Server"
    wsjf: 4.00
    bcps: 13
    capsule_dir: epics/e43-mcp-context-server
    supersedes: scripts/mcp-server.js (e21 prototype)
    note: >
      Build bigpowers-mcp — a project-specific TypeScript MCP server that
      parses all SKILL.md files via remark/unified into an entity-relation
      knowledge graph. Seven tools: index_skills, read_skill, build_skill_graph,
      search_skills, get_dependencies, get_git_context, validate_skill. Fills
      the Layer 3 (Technical Context) gap without needing ctxo or Markdown
      language plugins. Agents query the graph instead of reading individual
      files. Absorbs and replaces scripts/mcp-server.js (e21 prototype);
      e39s01 is re-scoped to emit its graph via e43's server. Source: MCP
      servers research (7 reference servers analyzed), tree-sitter/remark
      survey (2026-07-03).
  - id: e44
    title: "Spec Version Migration — migrate-version Skill"
    wsjf: 3.75
    bcps: 14
    capsule_dir: epics/e44-migrate-version
    note: >
      Closes a self-inflicted gap: downstream projects created at different
      bigpowers versions have stale spec formats with no upgrade path.
      migrate-version skill detects version gaps via fingerprint + stamp,
      loads applicable OKF spec-migration bundles, and applies one-shot
      ordered transforms with triple safety net (backup → dry-run → auto-commit).
      Only structural/breaking changes become named migrations. Integrates
      with survey-context for auto-detection at session start. Tests with
      golden fixtures (v1.x, v2.0.0, v2.20) + roundtrip validation (G-06).
      Depends on e39 (OKF v0.1) + e40 (validate-okf.sh). Source: grill-me
      design session (27 questions, 2026-07-03).
  - id: e40
    title: "Metrics Integrity — Honest, Additive, Benchmarkable"
    wsjf: 3.2
    bcps: 18
    capsule_dir: epics/e40-metrics-integrity
    supersedes: e35
    note: >
      Fixes a self-replicating defect: cycle-times.yaml is agent-self-reported
      wall-clock, hand-computed and ungated (outliers to 120 BCP/hr, templated
      rows, impossible identical timestamps); because bigpowers built itself,
      every downstream project inherits it. Replaces hand-arithmetic with a
      git-derived script (scripts/record-cycle-time.sh, drafted+verified this
      session) emitting two SEPARATED per-story fields — effort (idle-stripped,
      ADDITIVE via a global commit partition, Σ=project total) and lead_time
      (calendar latency, median-aggregated, never summed). Telemetry-first where
      the harness exposes usage (pi/Claude Code: tokens/cost/tier, naturally
      additive); git-effort as the portable fallback. Per-story OKF metrics
      bundle (specs/templates/story-metrics.okf.md) with aggregation tags; keeps
      the DORA four keys (market standard) but computes them from git/deploy
      events; validate-okf + provenance gate (gate on the pipeline, not the
      value). Absorbs & corrects e35 (whose "extend cycle-times.yaml" premise
      rested on the unreliable metric). Source: specs/RESEARCH-cycle-time-
      metrics.md, specs/CHANGE-REQUEST-e40-metrics-integrity.md, 2× red-team
      passes, session 2026-07-03.
  - id: e41
    title: "Public Receipts — Live Evidence Page"
    wsjf: 3.75
    bcps: 10
    capsule_dir: epics/e41-public-receipts
    note: >
      Change request 2026-07-03. Renders bigpowers' own quality evidence —
      compliance score (e31), golden-story pass@k (e37), honest git-derived
      metrics (e40), traceability matrix (e38) — as a public /receipts page
      on the e28 docs site. The differentiation play: competitors CLAIM
      discipline, this page PROVES it. WSJF (BV 8 + TC 4 + RR-OE 3) / JS 4
      = 3.75 — highest in the backlog, but hard dependencies on e28 + e40
      pin it to the v2.7x/v3.0 train. Every section degrades to an explicit
      "not yet measured" state when its upstream epic hasn't landed —
      partial data is fine, fabricated data never (e40 honesty rule).
  - id: e42
    title: "Showcase Repo — Worked Example Built With bigpowers"
    wsjf: 2.5
    bcps: 10
    capsule_dir: epics/e42-showcase-repo
    note: >
      Change request 2026-07-03. Small real app in a separate public repo
      built end-to-end with bigpowers, full specs/ cockpit committed —
      including one feature via the 8-step cycle and one bug via the
      investigate-bug trail. Lets prospects read a worked example before
      installing. WSJF (BV 6 + TC 2 + RR-OE 2) / JS 4 = 2.5. Pulled into
      v2.7x (was v2.8x) — it's the only epic that answers "why should a
      stranger adopt this?" — landing right after e41 means the launch
      train ships site + receipts + worked example together. Doubles as
      e37 golden-story fixture material.
  - id: e45
    title: "OKF Completion — Wikis, Verification Reports, Viz Graph"
    wsjf: 3.0
    bcps: 8
    capsule_dir: epics/e45-okf-completion
    note: >
      Closes remaining OKF surface area named in IMPACT-e38-okf-adoption.md
      but not yet owned by any epic. Produces: epics-wiki + adr-wiki as
      rendered OKF-concept pages; verification-reports-as-OKF (type:
      Verification with score/gate frontmatter); bug-registry as OKF
      concept documents; viz.html graph companion for visual-dashboard
      (renders the entity-relation graph from e43's build_skill_graph output
      as a browsable interactive force-layout). ~8 BCP, slotted in v2.8x
      after e39+e43+e44 land the OKF foundations. WSJF (BV 5 + TC 3 +
      RR-OE 4) / JS 4 = 3.0.

  - id: e46
    title: "Risk-Based Verification — TEA-Informed Test Depth"
    wsjf: 3.4
    bcps: 10
    capsule_dir: epics/e46-risk-based-verification
    note: >
      Three TEA mechanisms surfaced in the e38 audit: risk-based test depth
      (P0–P3 scaling verify-work), NFR evidence gate (performance/reliability/
      operability go/no-go), and tea.md reference doc. Knowledge tiering
      assigned to e45. Source: PLAN-AUDIT red-team gap list, BMAD TEA repo
      cached at ~/.opensrc/repos/github.com/bmad-code-org/bmad-method-test-
      architecture-enterprise/main.

  # ── DONE — shipped as v2.30.0 ───────────────────────────────────────
  - id: e26
    title: "Security-Review Lifecycle Integration"
    wsjf: 4.2
    bcps: 21
    capsule_dir: epics/e26-security-review
    note: >
      Weave security gates into build-epic, plan-work, plan-release, audit-code,
      request-review, fix-bug, validate-fix, verify-work, and release-branch.
      Ships core security-review skill with reference files + 9 targeted skill edits.
      All 7 stories done.


  # ── DONE — shipped as v2.45.1 (Deepening, 2026-07-02) ────────────────
  - id: e30
    title: "Architecture Quick Fixes"
    wsjf: 6.5
    bcps: 10
    capsule_dir: epics/archive/e30-arch-quick-fixes
    note: "Delivered: get_phase() 3 missing skills, broken tech-architecture refs, stale .bak, 43 tautological verify commands replaced. plan-checker gate story closed as already-satisfied (assess-impact gate pre-existing in build-epic)."

  # ── DONE — shipped as v2.29.0 (Methodology) ──────────────────────────
  - id: e25
    title: "Migrate-Spec Methodology Enhancements"
    wsjf: 3.0
    bcps: 10
    capsule_dir: epics/e25-migrate-spec-enhancements
    note: "Six additive enhancements from GSD/spec-kit/BMAD migration learnings: ID tracking, trace output, mandatory handoff, adversarial review, two-pass gate, methodology template"

  # ── DONE — shipped as v2.28.0 (2026-06-22) ──
  - id: e22
    title: "Skill Catalog CI Gate"
    wsjf: 5.5
    bcps: 2
    capsule_dir: epics/e22-skill-catalog-ci-gate
    note: "machine-runnable health check across all SKILL.md verify commands + CI integration"
  - id: e23
    title: "Benchmark Infrastructure"
    wsjf: 4.5
    bcps: 3
    capsule_dir: epics/e23-benchmark-infrastructure
    note: "fills broken evolve-skill dependency; defines specs/benchmarks/ schema + run-benchmark skill"
  - id: e24
    title: "Planning Context Capsule"
    wsjf: 4.0
    bcps: 3
    capsule_dir: epics/e24-planning-context-capsule
    note: "planning-context.yaml persists elaborate-spec output across scope-work → slice-tasks → plan-work"
  # ── DONE — 2.8.0 and earlier ──────────────────────────────────────────────
  - id: e20
    title: "Build-Epic Ergonomics & Flow Polish"
    wsjf: 3.4
    bcps: 5
    capsule_dir: epics/e20-build-epic-ergonomics
    note: "Slight downshift as CI/Deploy/Validate are more urgent."
  - id: e21
    title: "MCP-Native Discovery & Tool Integration"
    wsjf: 2.9
    bcps: 8
    capsule_dir: epics/e21-mcp-discovery-tools
    note: "Strategic but high JS (8) defers to future release."
  # ── DONE — e01-e19 ──────────────────────────────────────────────────────────
  - id: e01
    title: "Security"
    wsjf: 4.5
    bcps: 4
    file: epics/e01-security.yaml
    mode: flat
  - id: e02
    title: "Verification & Evals"
    wsjf: 4.3
    bcps: 4
    file: epics/e02-verification-evals.yaml
    mode: flat
  - id: e03
    title: "Discovery & Planning"
    wsjf: 4.0
    bcps: 2
    file: epics/e03-discovery-planning.yaml
    mode: flat
  - id: e04
    title: "Ergonomics"
    wsjf: 3.8
    bcps: 3
    file: epics/e04-ergonomics.yaml
    mode: flat
  - id: e05
    title: "Context Isolation + Routing"
    wsjf: 3.3
    bcps: 3
    file: epics/e05-context-isolation-routing.yaml
    mode: flat
  - id: e06
    title: "Taxonomy & Provenance"
    wsjf: 3.0
    bcps: 3
    file: epics/e06-taxonomy-provenance.yaml
    mode: flat
  - id: e07
    title: "Architectural Complexity"
    wsjf: 2.8
    bcps: 3
    file: epics/e07-architectural-complexity.yaml
    mode: flat
  - id: e08
    title: "Wave Execution"
    wsjf: 2.5
    bcps: 1
    file: epics/e08-wave-execution.yaml
    mode: flat
  - id: e09
    title: "Self-Evolution"
    wsjf: 2.0
    bcps: 11
    file: epics/e09-self-evolution.yaml
    mode: flat
  - id: e10
    title: "Stack Profiles"
    wsjf: 1.8
    bcps: 2
    file: epics/e10-stack-profiles.yaml
    mode: flat
  - id: e11
    title: "Sustain Cleanup"
    wsjf: 1.0
    bcps: 10
    file: epics/e11-sustain-cleanup.yaml
    mode: flat
    note: "Ad-hoc execution 2026-06-10 — see specs/tech-architecture/PLAN-sustain-cleanup.md"
  - id: e12
    title: "Pi Support"
    wsjf: 5.3
    bcps: 8
    capsule_dir: epics/e12-pi-support
    note: "Change request 2026-06-18 — pi agent harness skill generation and package config"
  - id: e13
    title: "Catalog Integrity & Index Automation"
    wsjf: 7.7
    bcps: 3
    file: epics/e13-catalog-integrity.yaml
    mode: flat
    note: "Delivered: lockfile regeneration (62/62), SKILL-INDEX auto-gen, stale skill cleanup."
  - id: e14
    title: "Release Hardening & Quality Metrics"
    wsjf: 6.0
    bcps: 3
    file: epics/e14-release-hardening-quality.yaml
    mode: flat
    note: "Delivered: release-branch solo fallback, fix-rate KPI, catalog audit (62/62)."
  - id: e15
    title: "CI/CD Pipeline & Package Publishing"
    wsjf: 5.8
    bcps: 5
    capsule_dir: epics/e15-ci-pipeline-publishing
    mode: flat
    note: "Delivered: wire-ci, publish-package, CI verification step in release-branch + develop-tdd."
  - id: e16
    title: "Deployment Pipeline"
    wsjf: 5.6
    bcps: 5
    capsule_dir: epics/e16-deployment-pipeline
    mode: flat
    note: "Delivered: deploy + smoke-test skills and deployment pipeline."
  - id: e17
    title: "Data Contract Validation"
    wsjf: 5.0
    bcps: 3
    capsule_dir: epics/e17-data-contract-validation
    mode: flat
    note: "Delivered: validate-contracts with JSON Schema, key-set comparison, data shape validation."
  - id: e18
    title: "Quality Gates & Impact Analysis"
    wsjf: 4.8
    bcps: 5
    capsule_dir: epics/e18-quality-gates-impact
    mode: capsule
    note: "Delivered: audit-code gate, enforce-first F.I.R.S.T integration, assess-impact lightweight mode, universal checkpoint pattern, auto-timing instrumentation."
  - id: e19
    title: "Quick-Fix Fast-Path"
    wsjf: 4.7
    bcps: 3
    capsule_dir: epics/e19-quick-fix-fast-path
    mode: capsule
    note: "Delivered: quick-fix skill with entry criteria, guardrails, fast-path workflow."
