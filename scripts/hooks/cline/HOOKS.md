# Cline Hook Templates (Wave A)

Shipped under `scripts/hooks/cline/` — copy to user config in Wave B.

## Events

- `tool.execute.before`
- `tool.execute.after`
- `session`
- `message`
- `permission`

## Security

Templates use fixed guard logic only — no dynamic eval.

